package main

func main() {
	// We can block and wait until something is sent on a channel using the
	// following syntax:
	//<-ch(where ch is the channel name)
	// This will block until it pops a single item off the channel, then continue,
	// discarding the item.

}
